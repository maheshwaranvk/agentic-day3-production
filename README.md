- Show how to run the app:

```bash
python app.py
```
.env file will not be committed to the repository, so you will need to create it yourself and add the following variables:

```OPENAI_API_KEY=your_openai_api_key
```